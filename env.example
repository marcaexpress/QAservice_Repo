## --- VARIABLES DE ENTORNO PARA DESPLIEGUE ROBUSTO ---
# Database (Neon para producción, local para desarrollo)
DATABASE_URL="postgresql://neondb_owner:password@ep-winter-dawn-ada6oavd-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require"
SHADOW_DATABASE_URL="postgresql://neondb_owner:password@ep-winter-dawn-ada6oavd-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require"

# Authentication
NEXTAUTH_SECRET="cambia-esto-en-produccion"
NEXTAUTH_URL="https://tudominio.vercel.app"
JWT_SECRET="cambia-esto-en-produccion"

# Google OAuth
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# CMS
CMS_EDITOR_ENABLED=true
CMS_AUTOSAVE_INTERVAL=5000
CMS_PREVIEW_ENABLED=true

# Environment
NODE_ENV=production

# Validación automática (agrega en tu código de inicio)
# if (!process.env.DATABASE_URL || !process.env.NEXTAUTH_SECRET || !process.env.JWT_SECRET) {
#   throw new Error('Faltan variables de entorno críticas para el despliegue');
# }
